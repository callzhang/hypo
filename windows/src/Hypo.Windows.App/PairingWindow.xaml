<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="Hypo.Windows.App.Shell.PairingWindow"
        Title="Hypo — Pair a Device"
        Width="460" Height="420"
        WindowStartupLocation="CenterScreen">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0"
               TextWrapping="Wrap"
               Foreground="#FF6B6B6B"
               Text="Devices running Hypo on this network. Open Hypo on the other device first." />

    <ListBox x:Name="Peers"
             Grid.Row="1"
             Margin="0,10"
             HorizontalContentAlignment="Stretch">
      <ListBox.ItemTemplate>
        <DataTemplate>
          <StackPanel Margin="2,6">
            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
            <TextBlock Foreground="#FF3A7D44" FontSize="11" Text="{Binding Status}" />
            <TextBlock Foreground="#FF9A9A9A" FontSize="10" Text="{Binding DeviceId}" />
          </StackPanel>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>

    <TextBlock x:Name="Message"
               Grid.Row="2"
               Margin="0,0,0,10"
               TextWrapping="Wrap" />

    <StackPanel Grid.Row="3">
      <Button x:Name="PairButton"
              Padding="12,6"
              HorizontalAlignment="Right"
              Content="Pair with selected device"
              Click="OnPair" />

      <Separator Margin="0,14,0,10" />

      <TextBlock TextWrapping="Wrap"
                 Foreground="#FF6B6B6B"
                 Text="Not on this network? Use a six-digit code instead." />

      <Grid Margin="0,8,0,0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBox x:Name="CodeBox" Grid.Column="0" Padding="6" MaxLength="6" />
        <Button x:Name="UseCodeButton"
                Grid.Column="1"
                Margin="8,0,0,0"
                Padding="12,6"
                Content="Use code"
                Click="OnUseCode" />
        <Button x:Name="ShowCodeButton"
                Grid.Column="2"
                Margin="8,0,0,0"
                Padding="12,6"
                Content="Show a code"
                Click="OnShowCode" />
      </Grid>
    </StackPanel>
  </Grid>
</Window>
